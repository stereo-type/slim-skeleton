{% extends 'layout.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('catalog_edit_form') }}
{% endblock %}


{% block content %}
    <div class="categories container content-body">
        {{ form_start(form) }}
        {{ csrf.fields | raw }}
        {{ form_end(form) }}
    </div>
{% endblock %}
